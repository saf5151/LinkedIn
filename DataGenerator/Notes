Assumptions:

Attributes recommend, searching, hiring, number, and ID's are stored as integers.
All other attributes are unrestricted strings.
All tables are named the same as shown in Relational Schema in Phase 2 write up.


Flow:

setup - user, company
pop - user, company
setup - everything else (lots will be blank)
pop - everything else

Go back:
Activity
Check getTable